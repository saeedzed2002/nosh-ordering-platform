# Nosh backend

The backend currently owns API metadata and service health only. Domain models,
authentication, media, and ordering behavior are intentionally deferred until
their respective roadmap phases.
